{
    "name": "trainstream-editor",
    "scripts": {
        "start": "cd auth && wrangler dev",
        "deploy": "cd auth && wrangler publish",
        "pretty": "prettier --write ./auth/*.{json,js} ./website/*.html ./website/assets/**/!(*.min).{js,html}"
    },
    "dependencies": {
        "prettier": "^2.2.1"
    },
    "prettier": {
        "tabWidth": 4,
        "semi": true,
        "singleQuote": true,
        "printWidth": 120
    },
    "license": "MIT"
}
